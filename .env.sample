# Container Registry Configuration
# Authenticate with: docker login $REGISTRY
REGISTRY=your-registry.example.com

# Public Domain
# The domain where the API will be accessible
DOMAIN=your-domain.example.com

# Kubernetes SSH Host
# SSH hostname for kubectl access to your cluster
KUBE_SSH_HOST=your-kubernetes-server
